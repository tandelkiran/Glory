import { CapitalFirstDirective } from './capital-first.directive';

describe('CapitalFirstDirective', () => {
  it('should create an instance', () => {
    const directive = new CapitalFirstDirective();
    expect(directive).toBeTruthy();
  });
});
